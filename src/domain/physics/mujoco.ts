/** @deprecated Compatibility shape only. Module locations belong to the consuming MuJoCo adapter. */
export interface MujocoPhysicsInitOptions {
  moduleUrl?: string;
  moduleUrlCandidates?: string[];
}
